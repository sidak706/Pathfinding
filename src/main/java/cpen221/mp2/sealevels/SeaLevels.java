package cpen221.mp2.sealevels;

public class SeaLevels {
    public static boolean[][] isSubmerged(double[][] terrain,
                                          GridLocation[] waterSources,
                                          double waterLevel) {
        /* TODO: Delete this comment and the next few lines of code,
            and then implement this function correctly.
            This skeleton code ensures that the GUI is usable. */

        int rows = terrain.length;
        int cols = terrain[0].length;
        boolean[][] isSubmerged = new boolean[rows][cols];
        return isSubmerged;
    }

    public static double[][] dangerLevel(double[][] terrain,
                                         GridLocation[] waterSources) {
        /* TODO: Delete this comment and the next line, then implement this function. */
        return null;
    }
}
